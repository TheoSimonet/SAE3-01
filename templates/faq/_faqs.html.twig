{% block body %}
    <div class="right__top">
        <p>
            <i class="bx bx-news"></i>
            F.A.Q
        </p>
    </div>
    <div class="right__center">
        <div class="faq__top">
            <div class="create">
                <a href="{{ path('app_faq_create') }}">Créer</a>
                <div class="search__icon"><i class='bx bx-plus'></i></div>
            </div>
        </div>
        {% for faq in faqs %}
            <div class="faq__item">
                <div class="faq__item__content">
                    <div class="faq__content__text">
                        <p class="faq__question"><i class='bx bxs-right-arrow'></i> {{ faq.question }}</p>
                        <p class="faq__reponse hide">{{ faq.reponse }}</p>
                    </div>
                    {% if is_granted('ROLE_ADMIN') %}
                        <div class="faq__item__actions">
                            <a href="{{ path('app_faq_update', {id: faq.id}) }}"><i class='bx bxs-edit-alt'></i></a>
                            <a href="{{ path('app_faq_delete', {id: faq.id}) }}"><i class='bx bxs-trash'></i></a>
                        </div>
                    {% endif %}
                </div>
            </div>
        {% endfor %}
    </div>

    <script src="{{ asset('assets/js/faq.js') }}"></script>
{% endblock %}