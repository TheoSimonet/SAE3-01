{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/css/projet/projet__form.css') }}"/>
{% endblock %}

{% block title %}
    Visualisation du stage n°{{ stage.id }}
{% endblock %}

{% block body %}
    <div class="right__top">
        <p>
            <i class='bx bxs-briefcase-alt-2'></i>
            Visualisation du stage n°{{ stage.id }}
        </p>
    </div>
    <div class="right__center">
        <div class="projet__show__item">
            <h2>{{ stage.titre }}</h2>
            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_ENTREPRISE') and app.user.id == stage.author.id %}
                <a id="edit" href="{{ path('app_stage_update', {id: stage.id}) }}"><i class='bx bxs-edit-alt'></i></a>
                <a id="delete" href="{{ path('app_stage_delete', {id: stage.id}) }}"><i class='bx bxs-trash'></i></a>
            {% endif %}
            <p id="projet__author"><span class="important">Personne ayant publié le stage:</span> {{ stage.author.firstname }} {{ stage.author.lastname }}</p>
            <p id="projet__desc"><span class="important">Description:</span> {{ stage.description }}</p>

            {% set dejaSelectionne = false %}
            {% for candidature in stage.candidatures %}
                {% if candidature.idUser.id == app.user.id %}
                    {% set dejaSelectionne = true %}
                {% endif %}
            {% endfor %}

            {% if (is_granted('ROLE_ADMIN') or is_granted('ROLE_ETUDIANT')) and not dejaSelectionne %}
                <a href="{{ path('app_candidature_new', {idStage: stage.id}) }}">Candidater</a>
            {% endif %}
        </div>
    </div>
    {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_ENTREPRISE') and app.user.id == stage.author.id %}
        <a href="{{ path('app_stage_retenues', {id: stage.id}) }}">Candidatures retenues</a>

        {% if stage.candidatures|length > 0 %}
            <h2>Liste des étudiants ayant candidaté à votre offre de stage :</h2>

            {% set x = 1 %}
            {% for candidature in stage.candidatures %}
                <h3>Candidature n°{{ x }}</h3>
                <p>{{ candidature.idUser.firstname }} {{ candidature.idUser.lastName }}</p>
                <p>Faite le {{ candidature.date|date("d/m/Y à H:i",  "Europe/Paris") }}</p>
                <a href="{{ path('app_stage_detail-candidature', {id: candidature.id}) }}">Voir plus</a>
                {% set x = x + 1 %}
            {% endfor %}
        {% else %}
            <h2>Aucun étudiant n'a candidaté à votre offre de stage.</h2>
        {% endif %}
    {% endif %}
{% endblock %}


