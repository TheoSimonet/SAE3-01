{% block body %}
    {{ form_start(form) }}
    <p class="projet__form__label__title">Titre du projet:</p>
    {{ form_row(form.titre, {'label': ' '}) }}
    <p class="projet__form__label__title">Description du projet:</p>
    {{ form_row(form.description, {'label': ' '}) }}
    <div class="projet__form__submit__box">
        {{ form_row(form.save, {'label': label_submit }) }}
    </div>
    {{ form_end(form) }}
{% endblock %}
